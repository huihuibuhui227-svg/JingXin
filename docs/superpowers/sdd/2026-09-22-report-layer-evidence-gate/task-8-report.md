# Task 8 报告:修订设计文档的三处前提

- 状态:**DONE**
- 文件:`docs/superpowers/specs/2026-09-21-jingxin-feature-redesign-design.md`(唯一改动的文件)
- 提交:`61a1548 docs: 按心理测量学审查修订设计文档三处前提`(1 file changed, 570 insertions —— 该设计文档此前未被 git 跟踪,故整文件入库,未附带任何其他路径)
- 无代码、无测试。本任务为纯文档,验证方式见文末「验证」。

---

## 一、三处修订逐条

### 修订 1:§6.3 阶段 1「三个数据集标定(先上线)」

**原文**(表格第 1 行):

```
| 1 | 三个数据集标定(**先上线**) | 低 |
```

**现文**(第 438 行):

```
| 1 | 三个数据集标定(**不上线**,仅作离线研究结论) | 低(仅限离线) |
```

并在表格正上方(第 434 行)加入指定的警示句:

```
> ⚠️ RecruitView 许可(CC BY-NC 4.0)明文禁止将本数据集**及在其上训练的模型**用于真实招聘、雇佣筛选或**心理画像**。故"数据集标定 → 先上线"这一组合被移除。详见增补 §1.1。
```

表格下方(第 442–447 行)按增补 §1.1 的**自身措辞**写入两条可选路径:

```
阶段 1 不再是上线路径:数据集系数只能作为离线研究结论存在,其合法去向只有两条(增补 §1.1):

- **(a) 有自有标注前 L2 不上线** —— 数据集标定可以做,结果只作为离线研究结论
- **(b) 数据集线严格限定为离线研究结论** —— 明确写入文档:数据集系数不得进入任何已上线功能,不得进产品、不得进销售材料

两条不互斥,可同时采用。对已上线功能而言,阶段 2 才是第一个可用的映射来源;上表阶段 1 的置信度上限"低"只在离线研究语境下成立。
```

**与增补一致的依据:** 增补 §1.1 的三条互证(CC BY-NC 4.0「Commercial use is strictly prohibited」/ 勾选项「neither this dataset **nor models trained on it** … employment screening, or **psychological profiling**」/ 政策第 2 条)以及其结论句「无论选哪条,『数据集标定 → 先上线』这个组合都必须从 §6.3 移除」。两条路径逐字取自增补 §1.1 的 (a)/(b),未改写、未弱化 —— 特别是 (b) 里「不得进入任何已上线功能,不得进产品、不得进销售材料」这半句是三处许可语言里最硬的一条,保留完整。警示句的「及在其上训练的模型」直接对应许可勾选项原文,是「数据标定 = models trained on it」这一等同关系的落点;没有它,读者会以为只要模型不直接用于筛选就合规。

**关于「低(仅限离线)」这一处:** 置信度列原为「低」。只删「先上线」而保留一个光秃的「低」,会让这一行读起来仍像一条生产映射路径。改为「低(仅限离线)」与紧随其后的说明句一致。这是同一行内的最小澄清,未动阶段 2/3 两行。

### 修订 2:§4.4 理由 1

**原文**(第 245 行):

```
1. **跨语言性质不同**:声学特征可跨语言迁移,语言特征不能。三个数据集全是英语,中文词汇特征一个都借不上。合并会让这个差别隐身。
```

**现文**:

```
1. **跨语言可迁移性不对称(仅适用于非 f0 类特征)** —— 语言特征不能跨语言迁移。注意:f0 类特征是**例外**,普通话的词汇声调压缩了情感语音中的音高变异,故 f0 类特征同样不可跨语言直接搬运(增补 §1.2)。三个数据集全是英语,中文词汇特征一个都借不上。合并会让这个差别隐身。
```

**与增补一致的依据:** 被删掉的正是增补 §1.2 点名的那个前提 ——「**声学特征可跨语言迁移,语言特征不能**」。替代文本用增补 §1.2 的原话机制描述(「普通话的词汇声调压缩了情感语音中的音高变异」,校准集英语 → 部署中文,「同一列的含义在标定与部署之间变了」)并指向增补 §1.2。

**关于 f0 例外的范围(自审重点):** 前提被显式限定在标题的括号里 ——「(仅适用于非 f0 类特征)」—— 因此「声学特征可跨语言迁移」这句话不再是无条件断言,而 f0 被单列为**例外**。文中「f0 类特征同样不可跨语言直接搬运」只对 f0 类成立,没有外推到全部声学特征。措辞用「直接搬运」(增补 §6.4 的说法)而非「不可迁移」,避免把「需要重新标定」说成「完全不可用」。

**保留原句尾的处理说明:** 原第 1 条末两句「三个数据集全是英语,中文词汇特征一个都借不上。合并会让这个差别隐身。」被保留。理由:(1) brief 的指令是「删除第 1 条**中的该前提**」,被删的是前提,不是整条;(2) 本节标题是「**不与 L0-A 合并的四个理由**」,尾句是「为什么要分开」的落点,删掉会让第 1 条只剩事实陈述、不再支撑「不合并」;(3) 该句只谈**词汇特征**(语言侧),与新增的 f0 例外语义上不冲突 —— f0 的域偏移是「同一列含义变了」,词汇特征的是「一个都借不上」,两者都是「不可直接搬」的不同成因。第 2/3/4 条(失败模式不同 / 隐私等级不同 / 时间线不同)原样保留,未动一字。

### 修订 3:§6.2 补伦理审查(强制项)

**原文**(第 428 行,其后直接接「允许缺标注」):

```
- 具体量表版本与授权需确认后使用
- **允许缺标注**——L2 的设计前提就是标注可迟到、可部分
```

**现文**(在第 428 行之后插入一条):

```
- 具体量表版本与授权需确认后使用
- **⚠️ 强制前置项:** 按《科技伦理审查办法(试行)》第 2 条第(一)项,以人为研究参与者(含"利用个人信息数据")的科技活动**必须通过科技伦理审查**。采集候选人自评量表用于标定落入该条,2023-12-01 已施行。此项与量表授权并列,不得省略。
- **允许缺标注**——L2 的设计前提就是标注可迟到、可部分
```

**与增补一致的依据:** 增补 §1.3 要求「在 §6.2 标注为**强制前置项**,与量表授权并列」,并给出三条要素:法条依据(第 2 条第(一)项)、触发条件(以人为研究参与者,含「利用个人信息数据」)、生效日(2023-12-01)。三者齐备。

**「强制」而非「建议」的落点(自审重点):** 用词是「**强制前置项**」「**必须**通过科技伦理审查」「**不得省略**」,并且「与量表授权**并列**」——即与一条本来就写着「需确认后使用」的硬性授权要求同级,不是附加建议。没有出现「建议 / 应当考虑 / 宜」这类弱化词。

**插入位置的形式选择:** 该句放在 §6.2 的项目符号列表内(作为 `- **…**` 一项),而不是用 `>` 引用块。brief 里 Step 1/2/3 的文本都用 `>` 渲染,但 Step 2 的替换对象本身是一个有序列表项(`1. …`),可见 brief 的 `>` 是「以下是文本」的引用记号而非格式要求;在列表中间插入引用块会在 Markdown 里切断列表。Step 1 的那句是在标题与表格之间,不是列表内部,故保留为 `>` 警示块(见下「格式」)。

---

## 二、§6.3 表格上方加入的那一句

见修订 1。逐字采用 brief 给出的文本,只在句末保留指向增补的「详见增补 §1.1」。位置上它紧贴表格上沿、位于 `### 6.3` 标题之下,是读者读到「三级回退」时先看到的东西 —— 这样「阶段 1 不上线」不会被误读为一次普通的措辞调整。

---

## 三、范围外的现存矛盾(仅报告,未改动)

按 brief「发现其他不一致只报告、不修」的要求,以下都**未动**:

1. **§6.3 标题「三级回退(不阻塞上线)」在修订后读起来偏松。** 阶段 1 退出生产路径后,生产侧的第一个映射来源变成阶段 2(依赖自有标注),而阶段 2 的量在短期内是零。若按「L2 标定不是上线闸门、缺 L2 不阻塞上线」读,标题仍成立;若按「三级回退保证上线时总有映射」读,则与增补 §1.1 路径 (a)「有自有标注前 L2 不上线」相抵。我已在表格下方写明「对已上线功能而言,阶段 2 才是第一个可用的映射来源」以消歧,但标题本身未改。**建议由控制者决定是否把标题改为「四级?/二级回退」或加限定。**

2. **§8 第 5 条(第 520 行)「L2 上线的必要条件是一份含样本量、置信区间、分布对齐检查的标定报告」** —— 修订后「L2 上线」还多出两个前置:许可(§6.3)与伦理审查(§6.2)。该句本身不与之矛盾(它是必要条件之一),但没有把它们列为「上线必要条件」。同类的还有 §9 的 M5 行「L2 标注机制 + 标定流程 + 三级回退」,未标 M5 被许可与伦理审查卡住(增补 §9 已写明)。

3. **§11 待定项第 2 条(第 556 行)「具体用哪份中文大五自评量表(版本与授权待确认)」** —— 这是与 §6.2 直接对应的开放问题条目,现在少了「通过科技伦理审查」这一项,读者只看 §11 会以为这件事的待办只有版本与授权。§6.2 已补,§11 未补。

4. **增补 §1.2 与 task-8-brief Step 2 在「理由条数」上的措辞差异(已在本次执行中按 brief 定案,记录备查)。** 增补 §1.2 末句为「结论(文本层与声学层分开)保留,**理由替换为剩下的三条** —— 失败模式不同 / 隐私等级不同 / 时间线不同」,字面可读作第 1 条被删、只剩三条;brief Step 2 则为「替换为[改写过后的第 1 条],**保留其余三条**」,即仍是四条。我按 brief 执行(§4.4 标题「四个理由」因此仍然成立)。若控制者的原意是删条,则需再补一次删条改动,并把标题改为「三个理由」。

5. **§1.3「离线实验的可用结论」(第 82 行起)在 RecruitView 上跑 2011 条数据得 macro r** —— 与修订后的表述**不冲突**(它是离线研究结论,正是路径 (b) 允许的去向),此处仅作为「文档中确实存在以数据集为对象的结论」的确认记录,提醒后续不要把这些系数搬进任何上线路径。

---

## 四、格式一致性

- 标点:文档通篇用 ASCII `"` `(` `)` `:` `,` 加全角 `、` `。`(实测 `"` 174 处、无弯引号)。三处新增文本沿用同一套字符,提交前用脚本核对过,新文本未引入弯引号。
- 换行:全文件 LF,无 CRLF;文末保留换行。§6.3 表格 `|` 列数与既有一致(每行 4 个管道符)。
- `>` 引用块:目标文档此前**没有**任何 `>` 块(grep 0 命中)。§6.3 那句按 brief 的渲染采用 `>`,作为一条独立的警示引出块;§6.2 的插入项因位于列表内部而用列表项形式,以免切断列表。若控制者要求全文样式统一,可以去掉 `>` 改为普通段落或加粗行 —— 内容不受影响。

---

## 五、验证(本任务无测试,以人工核对替代)

1. **逐条对照 brief Step 1–3 的指定文本**:三处替换/新增文本逐字比对,包括 Step 1 的警示句与 Step 3 的伦理审查句。
2. **对照增补**:§6.3 ↔ 增补 §1.1(不上线 + 两条路径原文);§4.4 ↔ 增补 §1.2(f0 例外);§6.2 ↔ 增补 §1.3(强制前置项)。
3. **范围核对**:`git show --stat HEAD` = 1 file changed,只有目标文件;未触发 369 个未跟踪文件与 64 个 deleted `.pyc`。提交身份按指令用 `-c user.name/-c user.email` 内联传入;无 amend、无 rebase。
4. **全文扫描**(确认无残留对立表述):`grep 上线 / 跨语言 / 伦理 / 授权 / 数据集标定` 全部命中处已逐一读过 —— 除上文第三节列出的 3 条范围外观察外,无其他处仍在主张「声学特征可跨语言迁移」或「数据集标定先上线」。
5. **格式核对**:LF、标点字符集、表格列数(脚本核对,见第四节)。

---

## 六、自审结论与遗留顾虑

- §6.3 现已与增补 §1.1 一致:阶段 1 不进生产路径,两条可选路径按增补自身措辞写入,「数据集标定 → 先上线」组合已移除。
- §4.4 理由 1 准确陈述 f0 例外,**未**把它外推到全部声学特征(限定语在条目标题内),同时保留了「不合并」这一节的论证链。
- §6.2 新增句用「强制前置项 / 必须 / 不得省略」,与量表授权并列,是强制而非建议。

**遗留顾虑(按重要性):**

1. 第三节第 4 条 —— 增补 §1.2 的字面意思若为「删掉第 1 条」,则本次按 brief 的「改写并保留」与增补字面有偏差,需控制者一句话定案。
2. 第三节第 1 条 —— §6.3 标题「不阻塞上线」在阶段 1 出局后语义偏松,已用正文消歧,标题未改(超出本任务范围)。
3. §11 第 2 条待定项未同步加上伦理审查,读者可能只读待定列表而漏掉这条强制前置项。
4. §4.4 保留了「三个数据集全是英语,中文词汇特征一个都借不上。合并会让这个差别隐身。」这一决定已在修订 2 中说明理由;若控制者认为 brief 的替换文本应完整取代整条(即删除该尾句),这是一处 5 秒可改的差异。

---
---

# 修复轮(审查反馈 2 Important + 4 Minor)

- 提交:`298220f docs: 同步设计文档中因三处前提修订而陈旧的表述`(1 file changed, 8 insertions(+), 6 deletions(-),仍只含目标文件)
- 审查结论:三处指定修订本体判为逐字忠实;本轮修的全部是「三处修订让别处旧文字变陈旧/自相矛盾」。
- **上一轮报告 §五.4 的「没有别处」结论已撤回**(见第五节声明)。本轮的完整清单见第六节。

---

## 一、Important 1 —— §6.6 每场会话输出示例 JSON(现 :473)

**改前**

```json
    "openness": { "estimate": 0.42, "ci": [-0.18, 0.78], "confidence": "低",
                  "basis": ["au_activity_index", "speech_rate"],
                  "n_labels_used": 0, "calibration_stage": 1 }
```

**改后**

```json
    "openness": { "estimate": 0.42, "ci": [-0.18, 0.78], "confidence": "低",
                  "basis": ["au_activity_index", "speech_rate"],
                  "n_labels_used": 128, "calibration_stage": 2 }
```

并在代码块之后新增一段(:484):

```
示例是**已上线会话**的输出:`calibration_stage` 只能取 2 或 3 —— 阶段 1(数据集标定)的产物只存在于离线研究,不得出现在任何已上线会话报告里(§6.3);`n_labels_used` 是本次映射所用到的**自有标注会话数**,阶段 2 起必须 > 0。
```

**为什么不是只把 `1` 改成 `2`:** 只改 stage 而留 `n_labels_used: 0`,会造出「阶段 2 + 零个自有标注」这个同样不成立的组合 —— 阶段 2 的定义就是「自有标注足够支撑校准」(§6.3 表)。两个字段是一组:`calibration_stage` 说用了哪一级,`n_labels_used` 说这一级的数据量,必须同时成立。改后的数值也满足 §6.3 该行的置信度上限(阶段 2 上限「中」,示例取「低」)。

**新增那段的作用:** 这份 JSON 是 M4 要照抄的接口形状(审查意见原话),所以规则要写进示例旁边,而不是只留在 §6.3 的表格里 —— 否则 M4 实现者读 §6.6 时看不到「stage 1 不得出现在上线报告里」。段内也把 `n_labels_used` 的语义钉死(自有标注会话数,阶段 2 起 > 0),避免下次再有人填出 0。

### §6.6 整段自查(审查要求:不止一个字段)

逐字段读完整段,结论如下:

| 字段 | 判定 |
|---|---|
| `session_id` | OK |
| `traits.openness.estimate` / `ci` / `confidence: 低` | OK —— 阶段 2 的上限是「中」,「低」合法 |
| `basis: [au_activity_index, speech_rate]` | OK —— 来自 L1 代表族 |
| `n_labels_used: 128` | 已修(原 0,与任何上线阶段都不成立) |
| `calibration_stage: 2` | 已修(原 1,数据集阶段) |
| `capabilities.logical_thinking` 无 `calibration_stage` / `n_labels_used` | OK 且应当如此 —— L2-b 从 L1 导出(§6.1),不经 L2 标定,故不携带标定阶段;若给它也加这两个字段反而错 |
| 顶层 `evidence_gaps` 与 `capabilities.*.evidence_gaps` 并存 | OK —— §6.6 末句「`evidence_gaps` 是一等输出」 |
| 整段是否还有第二处「数据集阶段」痕迹 | 无。示例中其余字段都不指向数据集线 |

**范围内没有第二处**,但整段读下来看到两处**与本次三处修订无关**的旧痕迹,已列入第七节(未改):`logical_thinking` 仍是改名前维度名;`basis` 里的 `response_latency_sec` 与 §11.1「无问题时间戳前降级或删」略有张力。

## 二、Important 2 —— §6.3 标题(现 :432)

**改前:** `### 6.3 三级回退(不阻塞上线)`
**改后:** `### 6.3 三级回退(阶段 1 限离线)`

采用审查给出的示例措辞。「不阻塞上线」在阶段 1 离开上线路径后不再成立:段内 :447 已写「对已上线功能而言,阶段 2 才是第一个可用的映射来源」,而阶段 2 要等自有标注 → 与增补 §1.1 路径 (a)「有自有标注前 L2 不上线」同向。全篇已 grep 确认「不阻塞上线」0 命中,无残留引用。

---

## 三、Minor 3–6

**3. §3 架构图 :145**

改前:`│  权重只能来自标定;三级回退(数据集 → 校准 → 本地化)       │`
改后:`│  权重只能来自标定;三级回退(数据集线仅离线 → 校准 → 本地化) │`

图中「数据集」原与「校准 → 本地化」并列呈同一条上线链,现插入「线仅离线」标注。改后该行显示宽度 62 列(与图底框 62 一致;原为 60)。**注:**该图 143/144 行原就比底框窄 1 列(61),属既有偏差,本次未动。

**4. §11 待定项第 2 条 :558**

改前:`2. **具体用哪份中文大五自评量表**(版本与授权待确认)。`
改后:`2. **具体用哪份中文大五自评量表**(版本与授权待确认;采集前须通过科技伦理审查,见 §6.2)。`

用「;」分隔而不嵌套括号,并写明「采集前须」以保持 §6.2 的强制性。

**5. §9 M5 行 :534**

改前依赖格:`M4 + 标注开始收集`
改后依赖格:`M4 + 标注开始收集 + **通过科技伦理审查(§6.2)** + 数据集线不得进上线路径(§6.3)`

与增补 §9 的「M5 卡在 §1.1 许可 + §1.3 伦理审查」对齐(增补写作 §1.1/§1.3,此处按设计文档自身的编号写 §6.3/§6.2)。

**6. §8 第 5 项 :522**

改前:`5. **标定报告作为门槛**:L2 上线的必要条件是一份含样本量、置信区间、分布对齐检查的标定报告。`
改后:`5. **标定报告作为门槛**:L2 上线的必要条件是一份含样本量、置信区间、分布对齐检查的标定报告 —— 以及两条并列前置:通过科技伦理审查(§6.2)、映射不得来自数据集系数(§6.3)。`

原文把「必要条件」写成单数,现在补成三条并列。

---

## 四、重新核对(与上轮同一套)

| 项 | 结果 |
|---|---|
| 换行 | CRLF 0、LF 572、文末有换行 |
| 引号风格 | 弯引号 0 处;全文 ASCII `"` 178 处,新增文本未引入弯引号 |
| 表格列数 | §6.3 表(436–440)、§9 表(529–533)每行 4 个管道符,全部 OK |
| 架构图对齐 | :145 宽度 62 = 底框 62(:146);:143/:144 仍为 61(既有偏差,未动) |
| 残留旧表述 grep | 「不阻塞上线」0、「calibration_stage": 1」0、「n_labels_used": 0」0、「数据集 → 校准」0 |
| 新文本 grep | 每条均 ≥1 命中(逐条列出见下) |
| 提交卫生 | `git show --stat` = 1 file changed,仅目标文件;身份内联传入;无 amend、无 rebase |

---

## 五、撤回上一轮报告 §五.4 的结论

上一轮报告 §五.4 写的是:「除上文第三节列出的 3 条范围外观察外,无其他处仍在主张『声学特征可跨语言迁移』或『数据集标定先上线』」。**该扫描不完整,结论作废。** 漏掉的正是本轮修掉的 :473(§6.6 示例的 `calibration_stage: 1`)与 :145(架构图把数据集线列入上线链)两处 —— 漏的原因是我上轮只 grep 了「上线 / 跨语言 / 伦理 / 授权 / 数据集标定」几个词,而这两处一个用字段名 `calibration_stage` 表达、一个用图示箭头「数据集 → 校准 → 本地化」表达,都不含「先上线」字样。本轮改用括号内三类各自的关键词集(上线/在线/产品/销售/招聘/商业/标定/数据集/许可/伦理/授权/迁移/跨语言/阶段/回退/权重/常模)逐行扫全篇,**不再给「没有别处」这类结论性判断**,只给逐处清单。

---

## 六、全篇重扫逐处清单(三类)

### (i) 数据集标定被当作在线/上线路径

**已改(4 处):** :145 架构图、:473 §6.6 示例、:522 §8 第 5 项、:534 §9 M5 行。

**判定可以不改(7 处,逐条给理由):**

| 行 | 内容 | 判定理由 |
|---|---|---|
| :174 | `au26_jaw_drop` …「再用**标定集的 p1/p99** 线性映射到 [0,1]」 | 这是 **L0/L1 的归一化映射**,不是 L2 阶段 1 的系数。它与许可禁止的「models trained on it」是否同解,取决于「由数据集统计量导出的阈值算不算模型」这一法律判断 —— 见下「需控制者定夺」 |
| :291 | §5.1 规矩 1「阈值必须来自标定集,跨会话固定」 | 同上;且规矩本身是「不许写死常数」的防线 |
| :295 | 正确做法「在标定集上拟合(如 `au12` 的 p90 取自**常模人群**),上线时固定使用」 | **同类风险最高的一处**:「常模人群」若指数据集,则该阈值进了上线路径。措辞出自本轮三处修订之前的原有设计,是否要加「(自有标定集)」限定是研究口径决定 |
| :303 | §5.1 规矩 5「标定阈值随模型版本号一起存储与版本化」 | 机制条款,不含来源,不构成「数据集 → 上线」主张 |
| :307 | §5.2 表头「τ(x) …来自标定集、跨会话固定(见 5.1 规矩 1)」 | 同 :291,引用条款 |
| :161 | `au1_inner_brow_raise`「是否减视频内中位数**由标定定**」 | 同 :291 类,是列级处置方式而非映射来源 |
| :141 | 架构图「族内选代表(**靠标定**)13 个」 | 与 :401「选哪个代表进 L2 也靠**标注**定,不能手挑」同指自有标注,语义一致,不改 |

**另 4 处用「标定」但经查指自有标注校准,判定一致(未改):** :112 §3 原则「权重由标定学习,禁止手填」;:114「特征集每次变更必须重跑标定」;:460 §6.5 规矩 1「权重只能来自标定」;:486 §6.6「权重与常模都必须来自标定」。依据::449 把 §6.5 那条直接接到阶段 2 的门槛上(「这与 6.5 的『权重只能来自标定』是同一条原则」,而其主语是自有标注的标定报告),故全篇「标定」在此语境下已由 :449 锚定为自有标注校准。**若控制者认为该锚定不够显式,建议在 :460/:486 补一个「(自有标注)」限定 —— 一处 2 字的改动,本轮未做,以免替研究口径下判断。**

**明确不属该类(未改):** :84 §1.3 离线实验(标题即「离线实验的可用结论」,是路径 (b) 允许的去向);:451/:453 §6.4「标定流程(离线,复用已建 harness)」,且 :453 自带「不可比的指标,其数据集系数不可直接搬」;:413 §6.1「L2-b 用途 = 招聘报告」—— 招聘用途本身被许可允许,被禁的是「用数据集或其训练出的模型」来做,而 §6.3 现已保证上线映射不来自数据集线,故一致;:119 §3.2「不做自动招聘决策」、:546 §10.1「不足以支撑招聘决策」与许可同向;:549 §10.4 域偏移是效度问题,非许可问题;:540「M2 可先于 M3 独立上线」指的是采集层 bug 修复,与 L2 无关。

### (ii) 伦理审查与许可被漏列在前置条件之外

**已改(5 处):** :429(上轮)、:434(上轮)、:522、:534、:558。

**判定可以不改(3 处):**

| 行 | 内容 | 判定理由 |
|---|---|---|
| :546–:551 | §10「已知限制与不宣称的东西」六条里没有一条提许可与伦理 | §10 不是前置条件清单,而是「不宣称什么」;:546 已写「不足以支撑招聘决策」。补一条「许可禁止心理画像/招聘筛选用途」会更完整,但那是**新增内容**,不是本次修订弄陈旧的文字 —— 是否加由控制者定 |
| :82–:96 | §1.3 离线实验未标注其许可状态 | 该节标题已含「离线」,且 §6.3 上方的许可警示块已给出全篇依据;补标是完善而非纠旧 |
| :119 | §3.2「不做自动招聘决策:系统输出倾向提示 + 置信度,不输出录用建议」 | 这是产品原则层面的自我约束,与伦理审查(流程前置)不是同一件事,不构成漏列 |

**已核对无遗漏的关键词扫描:**「许可 / 伦理 / 授权 / 法规 / 规章」全篇命中仅 :428、:429、:434、:558 四处,四处现已全部带有前置表述。

### (iii) 声学特征被说成可跨语言迁移

**已改(1 处):** :245 §4.4 理由 1(上轮,已限定「仅适用于非 f0 类特征」并把 f0 单列为例外)。

**判定可以不改(2 处):**

| 行 | 内容 | 判定理由 |
|---|---|---|
| :256 | 「**派生(中文特定,不可跨语言迁移)**」 | 与该类结论**同向**:它正说语言侧不可迁移。未改 |
| :453 / :549 | 把「语言」列为域偏移因素之一(数据集英语 / 系统中文) | 与该类结论同向,且是效度层面的表述,不主张声学特征可迁移 |

**关键词扫描:**「跨语言」命中 :245、:256,「声学」命中 :131(架构图列名)、:246–:248(L0-T 不合并的理由 2/3/4)。:246–:248 三条只说 ASR 错字 / 隐私 / 时间线,**无一条再以「可跨语言迁移」为理由**,与本轮修订一致。

---

## 七、范围外(不属三类)的两处观察,均未改

1. **:476 §6.6 示例仍用改名前维度名 `logical_thinking`。** 增补 §7.1 已拍板改名为「话语结构特征」,且仓库报告层已在 `7a4a543` 做了中性化。设计文档 §6.6 是接口示例,键名照抄会带出旧名 —— 属跨文档同步问题,不在本轮三类内。
2. **:477 §6.6 示例 `basis` 含 `response_latency_sec`,而 :557 §11.1 写「在具备[问题时间戳]之前,该指标降级为『首段静音时长』或删除」。** 示例与 §11.1 略有张力(示例把尚不可得的指标当既有 basis)。这是示例自身的老问题,与三处修订无关;整段自查时发现,记录备查。

---

## 八、需控制者定夺的一件事(本轮未改)

**由数据集统计量导出的 L1 阈值(§5.1 规矩 1/5、§5.2 τ(x)、:174 `au26` 的 p1/p99、:295 的「常模人群」)是否也受 RecruitView 许可的「models trained on it」约束?**

- 若「是」:这些位置都要加「(自有标定集)」或「(仅离线)」限定,而 :295 的「常模人群」必须改 —— 这会改变 L0/L1 的标定来源口径,属研究决定。
- 若「否」(阈值是数据集统计量、不是训练出的模型):则现状可保留,但建议至少把 :295 的「常模人群」改为「标定集」以避免读者误读为数据集人群。

本轮按「不在三处修订造成的陈旧之列 + 属研究口径判断」处理,**未改**,留待控制者一句话定案。
