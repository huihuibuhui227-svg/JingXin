# JingXin 下一步

- 更新日期:2026-09-25(**M1 + M1.5 + M2 完成并推送;M2 复审的 17/18/19 已清(M2.1);下一步 = M3**)
  - 2026-09-25(中秋)补:新增 **§8 工具成本与效率**(Claude Code 自身开销实测,与系统线无关,可跳读)
  - 2026-09-25 补:§3 第 17/18/19 条已做(M2.1),见 §5 索引里的计划与账本
- **主线:完善系统。不是重跑论文。** 目标是**让系统输出的每一句话都有依据**。
- 本文件是下一个会话的入口。看完 §0 与 §2 就能接着干。

---

## 0. 一句话现状

**①、M1、M1.5、M2 全部完成,已并入 `main` 并推送到 origin;T7 端到端验收已实跑通过。
M2 复审留下的 17 / 18 / 19 三条 Important 也已清(M2.1,2026-09-25)。下一步是 M3(L0 逐列重构)。**

- `main` 顶端 `489ed56` 之后:一个 §8 文档提交 + **M2.1 三个修复提交**(`6e63202` 第 17 条 / `88a1934` 第 18 条 / `c17c0ac` 第 19 条)——⚠️ **这四个提交还没推 origin**;前端仓 `~/JingXin-frontend` 对应两个提交(`5aa63c0` / `4232928`),同样未推
- **217 个测试全过**;合并门复核 **0 / 2835510**(与最初基线逐位一致)
- **M2 让报告第一次说出了真话。** 此前「本次观测覆盖」**恒为 0 / 20** —— 那不是"没证据",
  是**管线坏了**:加载器按"每模态文件名时间戳取最新"把**三场不同的会话**拼在一起,
  语音那份还读不出来,于是连接词密度根本进不了特征引擎。
  修完之后 T7 那一场是 **1 / 20**,而且那个 1 是**真测出来的**
  (连接词密度 2.4459 / 本场区间 0.2962–4.5956 / 有效样本量 5)。
  **1/20 是对的**:一帧脸产生不了时序统计,19 个槽本来就不该过门。
- **两个 CV 服务已复活**(M1.5):face(:8000)与 gesture(:8002)各发一帧真图都返回 **200**,
  并落出**文件名与首列都带 `session_id`** 的日志。
- **覆盖率往上走靠两件事,都不在 M2**:① M3 的逐列重构(让一场会话能产出更多可用的量);
  ② **一场会话采更多帧**(现在只有 1 帧,时序类的量至少要几十帧才有意义)。
- **下一步 = M3**(§6)。M2 的独立审查结论与**未修的四项**见 §3 第 17–22 条。

---

## 1. M1 交付了什么

| 交付 | 说明 |
|---|---|
| **vosk → FunASR** | 语音模块换成局域网 FunASR(`192.168.72.30:10095`,直连);**vosk 代码与 2.0 GB 模型已删**;四处识别点统一走一个 loop-safe 的 helper(`asyncio.to_thread`,因为客户端同步封装内部用 `asyncio.run`) |
| **`session_id` 贯通(写侧)** | `/interview/start` 铸号(`YYYYMMDD_HHMMSS_xxxx`)并建会话清单;id 显式下传三个模块;三份日志**首列 + 文件名**都带 id;face/gesture 不再每请求 mint uuid;id 从 **query 或表单**都认;非法 id 回 400(含路径穿越守卫) |
| **转写落盘(仓库外)** | `~/shared/jingxin_recordings/{session_id}/`:`session.json`(会话清单)+ `transcript.json`(原句 + **逐字毫秒时间戳** + 分段)。**原句不进仓库、也不进报告路径** |
| **`logic_keyword_density` → 真测量** | 采集时算「**连接词密度**」= 连接词数 ÷ 字数 × 100(每百字),标记表带 version;同名指标的**第二处定义已从报告层删净**;折算量程随新定义更新 |
| **并发与诚实性** | transcript 的读-改-写有按 `session_id` 的锁;ASR 失败不写 0、不写常量;文本过短不出值(不是 0) |
| **最终审查的修复波次(7 项)** | C1 产物遮蔽 / I1 有效样本量取指标自己的 `_sample_size` / I2 原句不落仓库 / **I3 文档口径** / **I4 报告头披露数据来源** / **D1 `load_markers()` 交深拷贝** / **D2 `log_prosody` 写失败改为抛出** / **D3 空串口径归一处** |

**证据与账本(已归档,仓库内):** `docs/superpowers/sdd/2026-09-24-m1-asr-session-id/`
—— `progress.md`(逐任务裁决、修复轮、**修复波次的落地表与反向复现记录**)、各任务报告。

---

## 2. 曾经挡在 T7 前面的硬伤:两个 CV 服务都被 mediapipe 1.0 打死(**已修:M1.5**)

**这一节保留诊断记录**(它是 M1.5 的立项依据);下面表格的第三列已更新为**修完之后的实测状态**。

| 服务 | 之前 | 现在(M1.5 后,实跑) |
|---|---|---|
| `-m voice_interaction.api.app`(:8001) | ✅ 能跑 | ✅ 能跑(未动) |
| `-m face_expression.api.app`(:8000) | ❌ **每个 `/analyze` 静默 500**(`VideoPipeline.face_mesh` 是惰性属性,启动不碰它 → 服务"起来了"、`/health` 正常,第一帧才炸) | ✅ **HTTP 200** 并落盘 `data/logs/face_au_log_<sid>.csv`(首列 = sid) |
| `-m gesture_analysis.api.app`(:8002) | ❌ **进程直接死**(`api/app.py:42-46` 在模块导入期取 `mp.solutions`,连 uvicorn 都没绑上) | ✅ **HTTP 200** 并落盘 `gesture_emotion_log_<sid>.csv`(`detected_hands: 2`,四个分析器 `is_valid: true`) |
| `-m report_frontend.report_generator` | ✅ 能跑 | ✅ 能跑(未动) |

根因(历史):`mediapipe 1.0.0` **整个 `solutions` 命名空间已删除**,只剩 `tasks`
(实测 `hasattr(mp,'solutions') → False`)。后果:`data/logs/` 里**永远不会出现**
`face_au_log_<sid>.csv`,**T7 的「face/voice 两份日志」判据在现环境不可能通过**。

**裁定(已执行):迁到 `tasks` API**(用户 2026-09-24 定)。三条路里选它而非"降到 mediapipe 0.10.x",
理由是干净、面向未来。**模型文件不随 pip 包发布**(实测 `find … -name "*.task"` 为空),
三个 `.task` 已下到 `models/mediapipe/`(被 `.gitignore` 排除,下载方式见 M1.5 spec §6.1)。

**两件迁移**留下的、**已登记但未验**的事(别当成已经验过,见 §3 跟进项 15/16):
① 封装的运行模式**固定在 VIDEO**,而 `~/shared/mp_frames/tasks.json` 基线是 IMAGE 录的 ——
`spec §10.4` 的**等价性门**还没跑;② `spec §3.4` 的数值位移要接进阈值文件(§12.1,归 M3)。

### 修完之后才跑的 T7

**先跑一遍这个(M1.5 已让两个服务可起):**

```bash
PY=~/miniconda3/envs/jingxin/bin/python
$PY -m face_expression.api.app      # :8000(启动即自检模型,缺失会拦在启动)
$PY -m gesture_analysis.api.app     # :8002
$PY -m voice_interaction.api.app    # :8001
```

**两个坑(不踩)**
1. **≥5 段回答,且内容各不相同** —— 同一段重复提交 → 每行密度相同 → `_std = 0` → **G2 按设计判
   「本次会话内无变化」**。G2 只要 ≥2 段;要**过门出分**还得满足 **G3 的样本量门槛 5**
   (每段回答一个样本,题库仅 8 题,默认的 10 不可达)。
2. **只能用 `/interview/start` 铸的 id** —— 自己编的 id 匹配不上报告侧的正则,报告里什么都看不到。

**另外一个环境缺口(不影响 T7):** TTS 是 `pyttsx3` → espeak → 靠系统 `aplay` 放音,而这个
WSL 里**没装 `aplay`**(`paplay`/`ffplay` 倒是在),所以交互式前端**听不见题**。
T7 用 curl 喂音频文件,不依赖它。

**核四条**
| 看什么 | 期望 |
|---|---|
| 落盘 | `ls ~/shared/jingxin_recordings/$SID/` → `session.json` + `transcript.json`(含逐字时间戳) |
| 对上号 | `data/logs/` 下 **face/voice/gesture 三份**日志文件名含 `$SID`、首列也是 `$SID` |
| 原句不出仓库 | 挑 transcript 里一句独特短语,`grep -rn "<短语>" ~/jingxin` → **0 命中** |
| 报告 | `~/miniconda3/envs/jingxin/bin/python -m report_frontend.report_generator --session-id $SID` → 「**话语结构特征**」出现「**连接词密度**」行并**过门出分**;报告头出现「**本场会话:`$SID`**」并逐模态给出**三态**(已读入 / 未读到数据 / 缺失,M2);全篇**无分数与评级**。**实测:T7 那场是 覆盖 1/20**(此前恒 0/20) |

---

## 3. 跟进项(按优先级)

| # | 事项 | 性质 |
|---|---|---|
| 1 | ~~**face/gesture 迁 `tasks` API**~~ — **✅ 已做(M1.5,代码 + 最终审查 + 修复波次全落地,待合并)**;设计与账本见 §5 | 已完成 |
| 2 | ~~**报告侧选日志仍按"每模态最新文件"**~~ — **✅ 已做(M2)**:改成"先定目标 `session_id`、再按 id 取每个模态";反向复现过(退回旧逻辑 → 精准红)。**实测 0/20 → 1/20** | 已完成 |
| 3 | ~~**`session_id` 的读侧不存在**~~ — **✅ 部分已做(M2)**:读侧按 id 选了,披露也补齐了。**但仍未做**:`grep session_id` 在 `feature_engine`/`research_mapper`/`visualizer`/`evidence_gate` 仍是**零命中**(那些模块至今不认 id);`refresh_manifest` 仍**零生产调用方**(M2 刻意不做,理由见 M2 spec §8 第 1 条) | 部分完成 |
| 4 | **voice 服务是进程级单例,不是按会话**(`api/app.py:57-59` 三个对象都不带 sid,`:307` 处被整体覆盖)→ 两会话并发互相污染 | 真实缺陷,比第 5 项严重 |
| 5 | 归属契约:"服务端为权威,**id 与当前会话不匹配回 409**" | 设计决定 |
| 6 | **实时路径丢指标**:`get_live_data` 不携带 `connective_density` → dashboard 该槽恒空(批量路径正常) | 功能缺口 |
| 7 | ~~**`logging_config.py` 未跟踪但被活代码 import**~~ — **✅ 已入 git(修复波次 I4)** | 已完成 |
| 8 | **另一处同形吞异常**(本轮未改,不在裁定范围内):`api/app.py:386-389` 的 `try: save_log() except Exception: pass` | 小 |
| 9 | `silence_energy_floor = 0.01` 是**定义性下限、未本机实测** → 现场增益偏低会复发"录满 timeout";**第一个要调的数是它** | 需实测 |
| 10 | 修后仍把约 `pause_threshold`(2.5 s)尾随静音喂给引擎;切干净需 VAD/M3 | M3 |
| 11 | `NONE` 桶跨天累积且所有无 id 客户端共享一个 pipeline | 留存/轮转,非正确性 |
| 12 | **只有铸造 id 可见**:客户端自定义 id 的文件不会被加载 | 契约,已记 |
| 13 | 名称统一:全仓已用「连接词密度」,但 `docs/` 里仍有「话语标记使用率」 | 文档收尾 |
| 14 | `text_avg_length` 指标无产出方;空串口径已在 D3 收口 | 清理 |
| 15 | **spec §10.4 的 VIDEO 模式等价性门(已知未验,别当成已验过)**:仓库封装**固定在 VIDEO 模式**,而 `~/shared/mp_frames/tasks.json` / `gesture_tasks.json` 基线是 **IMAGE** 录的 → 「原样拿封装去比基线」**在数学上不可能逐点一致**(实测 A 臂 face 1425/9082、gesture 152/1056,且检出形态不同:IMAGE gesture 姿态 25 帧 / VIDEO 28 帧)。**已实测 100% 的是另一条臂**:B 臂(同一适配层 + IMAGE 模式内层替身)face **9560/9560**、gesture **1119/1119** 逐点相等 —— **那是适配层写对了的证据,不是 A 臂的结论**。要做的是:另跑一套 **VIDEO 模式的新基线**(或明确接受这道门留到 M3) | 已知未验,M1.5 后 |
| 16 | **spec §12.1 的 M3 阈值/量程重登记**:spec §3.4 那张位移表(29 个 face 特征里 11 个相对位移 >10%、4 个 >25%;`au23_lip_compression` 55.8% / `au25_mouth_open` 46.4% / `gaze_direction_x` 28.0% / `au26_jaw_drop` 25.4%,**方向一致偏小** = 系统性偏置)要接进 `report_frontend/evidence_thresholds.json` 的 **`au` 族 + `scale_factors`**。按 spec §12.1 与**逐列重构一起做**(位移是模型包差异,改代码修不回来,只能接受并记进阈值依据) | M3 |

**完整 deferred minor 清单见账本** `progress.md` 各任务的 "deferred minor" 段。

### M2 独立审查(2026-09-24 深夜)的结论与未修项

**实跑 5 项全过**:端到端验收(审查者逐个独立复核了首列与 HTML)、合并门 **0/2835510**、
全量测试、前端 `npm run build`、**面板 spawn 路径真透传 `session_id`**(用了有区分度的构造:
先铸一场更新的空会话,再指定更旧但有数据那场 → 子进程与报告都指向后者)。
审查判 **Critical 1 / Important 4 / Minor 8**。**其中两条当晚已修并提交(`489ed56`)**:

- **C1(Critical,已修)**:`generate_report_live` 还在传旧形状 → `sources_disclosure` 抛
  `TypeError: string indices must be integers`,**而面板报 `success`**(静默失败)。
  这是 I 类漏改的典型:全仓**没有任何测试**碰那条路径。修法:抽出 `live_sources()` + 补钉子。
- **I1(Important,已修)**:`_none_bucket_rows` 的 glob 写成 `*_log_NONE_*.csv`,而 face/gesture
  的 NONE 文件**没有尾段**(`face_au_log_NONE.csv`)→ 盘上 **224 行数出来 0**,
  于是"报告头会点出 NONE 桶"在真实数据上完全不成立(而账本里那条 RealtimeAnalysis
  裁决的论据正是依赖它)。修完实测 **224 行**。

**未修的四项(明早接着看)**:

| # | 事项 | 性质 |
|---|---|---|
| 17 | ~~**I2**:只有 NONE 桶 / 显式指一个没有日志的 `session_id` → `generate_report` 返回 `''`,**一份报告都没有**,而面板还报 `success`(spec §6 行 1/2 未实现)~~ — **✅ 已做(M2.1)**:去掉 `raise ValueError`,空数据照样出报告并在头里逐条列出缺什么;`NONE` 桶的统计提到早退之前(只有 NONE 时也说得出口);报告模块**没产出就退出码非 0**(面板不再报「任务完成！」)。落地:`report_frontend/data_loader.py`(`target_session`)、`report_frontend/report_generator.py`(`main`) | 已完成 |
| 18 | ~~**I3**:前端两条读路径(`getStructuredReport()`、`runModule('report')`)**不带 id**;`/api/report/structured` 的 JSON 里**没有"这是哪一场"**(D2 的"写明"只落在 HTML 报告里)~~ — **✅ 已做(M2.1)**:`withSession` 收 override,两条读路径都带上 id;端点回传 `session_id` + `sources`,且**空数据不再回 error**;报告页新增披露卡(本场是哪一场 + 每个模态进来没有)。落地:`app.py`(`/api/report/structured`)、`~/JingXin-frontend/src/services/api.ts`、`.../pages/ReportPage.tsx` | 已完成 |
| 19 | ~~**I4(本次改动新引入的失败模式)**:科研链路仍无铸号可用(`research.start()` 不 setSessionId、`/research/start` 不铸号)→ 科研回答仍落 NONE;**先面试再科研时会被写成上一场面试的 `session_id`**~~ — **✅ 已做(M2.1,方案 A)**:`/research/start` 与 `/interview/start` 对称铸号 + `ensure_manifest`,前端 `research.start()` 存号。**端到端验收已实跑**(`~/shared/m21_acceptance.sh`,真 ASR):带科研号只落科研目录、带旧面试号确实会污染面试目录(反向复现成功)。落地:`voice_interaction/api/app.py` | 已完成 |
| 20 | **`RealtimeAnalysis` 不自行起会话**(M2 的 deviation)—— 复审判定**可以不改**(若原样实现 spec §5.3,会让实时演示页铸出"更新的空会话",反而把今天能用的读路径弄坏)。前提是 I1 已修(已修) | 已裁决 |

### 前端架构(用户 2026-09-24 问,建议按此顺序做,都留到 M3 之后)

| # | 做什么 | 为什么 |
|---|---|---|
| 21 | ★ **用 OpenAPI 生成前端 TS 类型** | 三个 FastAPI 服务**都已暴露 `/openapi.json`**,而前端类型是**手写**的 —— 这是"改后端前端不知道"的根,也是"逐步模块化加功能"的前提。**收益最大、成本最低** |
| 22 | **报告入口收口** —— 面板不再 spawn 子进程跑模块,改成一个明确的函数/路由 | 一次干掉两处已知坏点(`module_map["voice"]` 指向不存在的模块、报告模块 `__main__` 坏),且 `session_id` 传递变自然 |
| 23 | 删死依赖 `socket.io-client`(全仓零使用);决定实时走"逐帧 POST"还是真流 | 逐帧 POST 现在能跑,但美化/加功能时它会先成瓶颈 |
| 24 | **验收脚本两个卡死 bug**(复审发现):`~/shared/m2_acceptance.sh` 里 (a) `start_all.sh` 等 `npm run dev` 的子进程,5173 未先起时永远走不到断言;(b) 第⑤步 `webbrowser.open`→`xdg-open`→msedge 继承管道,`grep` 等不到 EOF | 小 |

**框架结论(别再纠结)**:前端 **React + Vite + TS 保持**;**FastAPI 保持,Django 不合适**(这里没有关系型数据库,状态是文件 + 进程内字典);**不需要换语言**(项目价值在 CV/音频分析,那是 Python 的地盘)。**不建议**加网关/换状态库 —— 前端直连 4 个服务在"单机单人用"下是对的(YAGNI)。

---

## 4. 必须带进后续轮次的经验

1. **"测试通过 ≠ 有约束力"这一路反复出现**。形态:抽样唯一性断言、fixture 恰好 100 字导致分母
   可被删掉、空输入契约无守卫、`listen_for_speech` 的新规则是死代码、只认 query 的 id 会静默
   降级成 NONE、**断言用了裸词「不同」而报告别处本来就有「不同指标的量纲……」**(I4 收尾时我
   自己踩的)。
   - 强制动作:**每个测试都要能说出"哪个生产改动会让它变红、红得对不对"**;说不出来就是没约束力。
   - 已常驻:`tests/test_assert_coverage.py` —— 但**它抓不到"缺断言"**,别当万能。
2. **反向复现是标准动作**:写完后把生产改动撤掉(整文件回退 / 外科式抽掉接线),确认新测试
   真的红,再恢复。本轮 5 项逐项做过。
3. **变异验证必须清 `__pycache__`**:同一秒内"写入→跑→`git checkout` 还原"会**跑变异字节码**。
4. **阈值/常量一律进数据文件**(带 `_provisional` + 依据)。注意**单位错配**:门槛是给帧级列设的
   (一场上千帧),而「连接词密度」每段回答一个样本、题库仅 8 题 → 该族单独登记 **5**。
5. **跨任务集成点要在计划里点名**:T3 改文件名没把 `data_loader` 的选取正则列进改动面,导致报告
   路径什么都加载不到 —— 只有审查者用真加载器跑了一遍才发现。
6. **文档口径要一起改**:I3 就是"spec/计划说 ≥2 段、代码门槛是 5"这种互相矛盾,靠 grep 才抓得到。
   **但账本里的历史记录不改** —— 改了就成篡改。
7. **WSL 内删文件不还 Windows 磁盘**:`D:\WSL\ext4.vhdx` 只增不减,回收需 Windows 侧
   `wsl --manage <发行版> --set-sparse true` 或 diskpart compact。

---

## 5. 关键文件索引

| 用途 | 路径 |
|---|---|
| **本文件** | `docs/下一步.md` |
| **M1 设计(权威)** | `docs/superpowers/specs/2026-09-24-m1-asr-session-id-design.md` |
| **M1 实施计划** | `docs/superpowers/plans/2026-09-24-m1-asr-session-id.md` |
| **M1 账本与各任务报告** | `docs/superpowers/sdd/2026-09-24-m1-asr-session-id/` |
| **M1.5 设计(权威)** | `docs/superpowers/specs/2026-09-24-m1-5-mediapipe-tasks-migration-design.md` |
| **M1.5 实施计划** | `docs/superpowers/plans/2026-09-24-m1-5-mediapipe-tasks-migration.md` |
| **M1.5 账本 + 各任务报告 + 最终审查 + 修复波次报告** | `docs/superpowers/sdd/2026-09-24-m1-5-mediapipe-tasks-migration/`(已归档进仓库;原来只在被 gitignore 的 `.superpowers/sdd/` 下) |
| **M2 设计(权威)** | `docs/superpowers/specs/2026-09-24-m2-read-side-session-alignment-design.md` |
| **M2 实施计划** | `docs/superpowers/plans/2026-09-24-m2-read-side-session-alignment.md` |
| **M2 账本 + 独立审查报告** | `docs/superpowers/sdd/2026-09-24-m2-read-side-session-alignment/`(**直接建在 git 跟踪的目录** —— 吸取"复审报告落在 gitignore 目录里差点丢"的教训) |
| **M2.1 实施计划(17/18/19)** | `docs/superpowers/plans/2026-09-25-m2-review-fixes.md` |
| **M2.1 账本(裁决 / 反向复现 / 验收原始输出)** | `docs/superpowers/sdd/2026-09-25-m2-review-fixes/progress.md` |
| **验收脚本(可自己跑)** | `~/shared/start_all.sh`(起全套 5 个服务)、`~/shared/t7_acceptance.sh`(T7)、`~/shared/m2_acceptance.sh`(M2 契约层)、`~/shared/m21_acceptance.sh`(M2.1:科研铸号与"张冠李戴") |
| **前端(独立仓库)** | `~/JingXin-frontend`(React + Vite + TS;`src/services/api.ts` 管 `session_id` 生命周期)。⚠️ 该仓库工作树里有**一批使用者先前未提交的改动**,提交时**只 add 你要提交的文件**,别 `git add -A` |
| ① 的设计增补 / 计划 / 账本 | `docs/superpowers/specs/2026-09-22-jingxin-report-layer-evidence-gate-design.md` 等 |
| 特征重构总设计 | `docs/superpowers/specs/2026-09-21-jingxin-feature-redesign-design.md` |
| 心理测量学审查(红线) | `docs/superpowers/specs/2026-09-22-测评回复_心理测量学审查.md` |
| 录制需求(三个硬前提) | `docs/superpowers/specs/2026-09-22-jingxin-recording-requirements.md` |
| ASR 适配层 | `voice_interaction/asr/`(`funasr_client.py` 为 vendor 副本;`funasr_engine.py` / `transcribe.py` / `transcript_store.py` / `connective_density.py` / `asr_config.json`) |
| 报告层 | `report_frontend/research_mapper.py` / `report_generator.py`(含 `sources_disclosure`) / `feature_engine.py` / `evidence_gate.py` / `evidence_thresholds.json` |
| CV 服务(已迁 `tasks`,活路径零 `mp.solutions`) | `face_expression/pipeline/detector.py`、`gesture_analysis/core/detectors.py`(两个封装 + `verify_models` + `close_detached`)、`gesture_analysis/api/app.py`(按会话 + TTL + `/reset`) |

**解释器固定为** `~/miniconda3/envs/jingxin/bin/python`(不可用 `~/huihui/bin/python` —— 缺 websockets 等)。

**合并门/防线(任何时候都要能过)**:
```bash
cd ~/jingxin/experiments/duration_audit
PY=~/miniconda3/envs/jingxin/bin/python
$PY reaggregate_normalized.py --stats legacy --out-dir /tmp/legacy_probe
$PY reaggregate_normalized.py --verify-legacy /tmp/legacy_probe   # 期望 0/2835510
```

---

## 6. 之后的主线(原顺序)

```
M1    ASR 换 FunASR + session_id 写侧贯通        ← ✅ 已并入 main 并推送
M1.5  face/gesture 迁 mediapipe tasks API        ← ✅ 同上
M2    L0 前置:读侧按 session_id 对齐            ← ✅ 同上(独立审查已在,未修 4 项见 §3 第 17–20 条)
M3    L0 逐列重构(54 列;语音 10 → 16,含逐字时间戳能白捡的语速/停顿/反应潜伏期)   ← 当前
      └ 含跟进项 16(阈值/量程重登记)与 M1.5 spec §3.4 的位移表
      └ 含跟进项 15(VIDEO 模式等价性门,已知未验 —— 别当成已验过)
M4    L1 指标层(36 个)
M5    L2 标注 + 标定      ← 卡在许可 + 伦理审查(两个硬前提,见下)
```

**M3 的输入已经就位**:M1 给了逐字时间戳(能白捡语速/停顿/反应潜伏期)、M1.5 给了**已量化的
探测器位移表**(29 个 face 特征里 11 个相对位移 >10%,且方向一致偏小)、M2 让"每列来自哪一场"
第一次可靠。**这三件正是逐列重构的前提。**

**M5 的两个前置(现在别碰):** ① RecruitView 许可禁止"数据集及其上训练的模型"用于真实招聘/
筛选/心理画像 —— **在数据集分布上拟合的参数(含 L1 阈值)同样属禁**,阈值只能来自物理下限或
自采数据(设计文档 §10 已记);② 采集自评量表**必须先过科技伦理审查**(强制项)。

---

## 7. 一句话提醒

**系统该宣称的是「结构化行为记录 + 低置信度特质提示」,不是「心理识别结论」。** ① 让系统停止说谎,
M1 让它重新有真话可说;精度天花板(r ≈ 0.33)不在这一轮的目标里。

---

## 8. 工具成本与效率(2026-09-25 中秋,实测)

**这一节跟 JingXin 系统本身无关,是 Claude Code 自身的开销。** 起因是 7 天账单折算后烧钱明显,
用 `session-report` 插件实测了一次。

**窗口:2026-09-19 ~ 09-25,16 场会话 / 14,043 次 API 调用 / 活跃 21.3 小时。**

### 8.1 成本模型:只有这一条公式

> **成本 ≈ 平均上下文 × 调用次数 × 单价**

实测:**145,428 token/次 × 14,043 次** = 20.5 亿 token。但 **98.4% 是缓存读取**。

按 DeepSeek 官方价(缓存命中 0.02 / 未命中 1 / 输出 4 元每百万 token,空闲时段):

| 项目 | 量 | 单价 | 成本 | 占比 |
|---|---|---|---|---|
| **缓存读取** | 2,018.3M | 0.02 | **40.4 元** | **40.7%** |
| 未缓存输入 | 23.9M | 1.00 | 23.9 元 | 24.1% |
| **输出** | 8.7M | 4.00 | **35.0 元** | **35.2%** |
| | | | **≈ 99 元/周** | |

**⚠️ token 份额 ≠ 成本份额。** 缓存读取占 98.4% 的 token,但单价只有 1/50,成本占比 40.7%。
**看报告不能只看总量。** 叠加峰谷后实际约 **134 元/周 ≈ 580 元/月**。

### 8.2 可优化的(按性价比排)

**① 峰谷挪时间 —— 免费,省最多 ★**

高峰时段(工作日 9:00-12:00、14:00-18:00)单价**全部 ×2**。实测工作时段:

```
9/22 周二  20:32-23:22   593M   空闲
9/24 周四  10:56-14:35   701M   ⚠高峰   ← 全周 34% 在这
9/24 周四  19:40-23:28   501M   空闲
```

**周四白天那一场是唯一的大额高峰消费。同样一场活挪到 18:00 之后跑,估计省 15–35 元/周。**
零成本、零风险、不用改任何配置。

**② 别让单条 prompt 跑到上千次调用**

| 调用次数 | 平均上下文 | prompt |
|---|---|---|
| **3,348 次** | 78,340 | `测评提示词_心理学专家审查.md` |
| 1,146 次 | 120,445 | `看一下jingxin下一步,开始下一步任务` |
| 1,077 次 | 147,470 | `继续,T5 该派就派` |
| 801 次 | 161,476 | `继续,T3 该派就派` |

一条 prompt 跑 3,348 次 = 每次都重读一遍上下文。**中途 `/clear` 或分段推进,直接把这条腿砍短。**

**③ 「该派就派」这个句式要加边界**

它在头部出现两次,合计 **14%**。而 **9,795 / 14,043 = 70% 的 API 调用来自 general-purpose
子代理**(78 个,平均每个跑 **126 轮循环**)。这句话明确授权了开放式扇出。

改法:派之前写死边界 —— **"派一个做 X,只返回 Y,不要继续探索"**。

**④ 上下文窗口(评估过,不建议动)**

配置是 `CLAUDE_CODE_MAX_CONTEXT_TOKENS: 1000000` + `deepseek-v4-flash[1M]`,实测有 prompt
平均 **331,436 token/次**(`按这个来,保留审查者,开工…`,333 次),1M 上限让它一直不触发 compact。

降到 250k–300k 理论上能压缓存读取,**但 compact 更频繁 = 更容易丢上下文,且压缩本身也耗 token。
收益不确定,不动。**

### 8.3 不该优化的(别白费力气)

- **输出那 35.2%**:8.7M ÷ 14,043 = **623 token/次**,这是写代码/写分析/写审查的正常产出量。压不动,硬压只会让活干得糙。
- **缓存单价已经 0.02 元/M**(全价 1/50)。优化空间在"量"不在"价",而"量" = 上下文 × 次数。
- **subagent 不全是浪费**:它隔离上下文,本身在保护主会话。真问题是任务规模,不是手段。

### 8.4 一个对账警告

报告里 **`cache_create = 0`**,很可能 `api.deepseek.com` 不回传该字段。**所以"98.8% 缓存命中"
是按 `cache_read ÷ total` 算的,不是计费口径。这份报告是 token 维度、不是账单维度 —— 对账请以
DeepSeek 账单为准,别拿它的数字去核。**

复现:

```bash
S=~/.claude/plugins/cache/claude-plugins-official/session-report/*/skills/session-report
node "$S/analyze-sessions.mjs" --json --since 7d > /tmp/session-report.json
```

(2026-09-25 那次的成品报告:`~/session-report-20260925-1220.html`,未进仓库。)
