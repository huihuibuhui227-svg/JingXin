# JingXin 下一步

- 更新日期:2026-09-24(① 完成并合并后)
- **主线:完善系统。不是重跑论文。** 论文那条线已单独停。目标是**让系统输出的每一句话都有依据**。
- 本文件是下一个会话的入口。看完 §0 与 §3 就能接着干。

---

## 0. 一句话现状

**系统不再说谎,而且恢复了诚实的呈现。** ①(报告层真实化与证据门)已合并进 `main`。接下来是 **M1(语音 ASR 落盘)**,它让报告重新有真话可说。

- `main` = `9c8d690`(① 的合并提交)+ `1a84266`(仓库卫生)
- **51 个测试全过**;采集层回归防线 **0 / 2835510 格差异**
- 分支 `fix/report-layer-evidence-gate` 已并入(20 个提交)

---

## 1. ① 交付了什么

| 拆掉的 | 换上的 |
|---|---|
| 假简历兜底(占**最高权重 0.4**)、三条硬编码代理、`BASELINE_FILL` | **证据门四关**(有值 → 非常量 → 样本够 → 非代理),先失败先返回 |
| 杜撰常模 `[60]*5` 与百分位机器、`_get_percentile_badge` | **35 条封停名单**(带解封条件)与置信度三值(「高」本轮结构上不可达) |
| 硬编码叙事层(「显示出良好的科研天赋」)、判推模板与 `inference_template` | 每维按 spec §5.4 呈现:**值 + 有效样本量 + 置信度 + 证据缺口** |
| **点分 + 五档评语**(1 个槽过门也能出「100.0 / 卓越」) | **覆盖事实 + 本场实测区间 + 置信度 + 依据**;无标定样本就不出综合分 |
| 「抗压与情绪稳定性」「面部紧张度」等措辞、眼动图与死代码 | 中性命名 + **全字符串字面量禁止词扫描**(含 `*.html`/`*.json`) |

**验证材料**:
- 门 4 报告:`D:\Shared\jingxin_gate4_final\report.html`(真实会话);`report_scored.html` 是**出分路径**的样例(真实数据不走那条)
- 账本、未决清单、修复证据:`docs/superpowers/sdd/2026-09-22-report-layer-evidence-gate/`(`progress.md` / `deferred-minors.md` / `final-fix-report.md`)

---

## 2. 三条已经踩过的坑(带进 M1–M5)

1. **测试通过 ≠ 测试有约束力。** 本轮栽了 **9 次**:断言与实现矛盾、测试全绿却掩盖功能全失效、改动前后都通过、**循环一次都没执行**、fixture 走不到被测路径、以及最后一条 —— **断言比 docstring 声称的弱**(属"缺断言")。
   - 已加 `tests/test_assert_coverage.py`(记录每个测试实际执行的行,与 AST 断言行求差)。
   - ⚠️ **它的盲区**:"缺断言"它抓不到 —— 别把它当成全部防护。
2. **过渡性"让链路跑通"的改动,必须检查跑通之后输出的是什么。** 曾为了防崩溃加防护,结果从"崩溃"变成"撒谎"。
3. **阈值与常量一律进 `report_frontend/evidence_thresholds.json`**,带 `_provisional` 与依据;禁止写成代码里的裸常量(scale factor 也要登记)。

---

## 3. 明天从哪继续:M1(ASR 落盘 + `session_id`)

**你交代过:语音部分的 vosk 要换成 FunASR。** 位置:

- `voice_interaction/api/app.py`
- `voice_interaction/pipeline/speech_recognition_pipeline.py`

已知可用信息(**开工前需复核**):FunASR 走 `192.168.72.30` 直连(无需隧道);systemd 服务名 `funasr-streaming`;逐字毫秒时间戳可用。这三项正是 M1 需要的。

M1 还要修的四件:

| # | 事项 | 说明 |
|---|---|---|
| 1 | **`session_id` 链路** | 三份日志现在都不写(部分 face 日志首列是 `session_id`、另一部分是 `timestamp`;语音与手势都没有)。实测曾拼出「5 月的脸 + 2 月的语音」 |
| 2 | `logic_keyword_density` | 现在仍走假简历兜底,M1 接上真转写后才会变成真测量(那时**改名「话语标记使用率」**) |
| 3 | `get_fused_latest_data()` | 会挑到 4 帧存根文件;若真实流程中常见,是 `data_loader` 的选档问题 |
| 4 | **新键必须带 `_std`** | 本轮修复发现:`_std` 查找此前只认 `_mean` 后缀 → **ASR 派生键会静默失去 G2 与观测区间**(已修)。接真转写后确认新特征键成对写入 |
| 5 | **删 vosk 模型(2.0 GB)与它的代码** | `vosk-model-cn-0.22/` 未被 git 跟踪,现在是语音模块的活依赖(`voice_interaction/api/app.py:60` 启动即加载)。**代码与模型放同一个提交**删,避免留下窗口期。⚠️ 在 WSL 内删文件**不会自动还你 Windows 侧磁盘** —— `D:\WSL\ext4.vhdx` 只增不减,回收要在 Windows 侧 `wsl --manage <发行版> --set-sparse true` 或 diskpart compact |

**关于综合分**:最终修复波次之后,报告**完全不再产出综合分与评级** —— 1/20 个槽过门时也只报覆盖(N/M 槽)+ 逐指标的本场实测区间 + 置信度上限。所以"覆盖率门槛"这条问题**已关闭**。将来若要在有标定样本后重新引入综合分,必须先有标定(M5),且呈现形态回到 spec §5.6 的「区间 + 置信度 + 依据」。

---

## 4. M3 的目标(别忘)

| 模态 | 现在 | M3 后 |
|---|---|---|
| 面部 | 146 列 | ≈22 |
| 手势 | **182 个特征键**(54 源列 × 6 统计量,无任何过滤) | ≈13 列 |
| 语音 | 10 列 | ≈16 |
| 合计 | 191 列 / 1410 维 | ≈54 列 |

**gesture 是唯一没有关键词过滤的模态**,且那 6 个统计量里含 `_trend` —— 设计文档 §1.2 实测(面部)该类二阶派生列与视频时长 |r| 达 0.77–0.84。

---

## 5. M5 的两个前置(现在别碰)

1. **RecruitView 许可明文禁止**将本数据集**及在其上训练的模型**用于真实招聘、雇佣筛选或心理画像 —— "数据集标定先上线"这条路已封(设计文档 §6.3 已改)。
   - **更严的一条(2026-09-24 新增)**:在数据集人群分布上**拟合出来的参数**(含 L1 阈值、`au26` p1/p99、`au12` p90 这类归一化参数)同样属"在其上训练的模型",**不得进入上线路径**。阈值只能来自物理下限或自采数据 —— `evidence_thresholds.json` 已经这么做(其依据写的是"物理下限,非标定值"),M5 延续这条线。
2. **采集自评量表必须先过科技伦理审查**(《科技伦理审查办法(试行)》第 2 条,**强制项**)。

详见 `docs/superpowers/specs/2026-09-22-jingxin-recording-requirements.md`。

---

## 6. 等你决定的几件事

| # | 事项 | 现状/我的建议 |
|---|---|---|
| 1 | **`experiments/` 下的未跟踪文件** | **已处理**:`experiments/results/`(1.1 GB / 10,128 文件)已进 `.gitignore`,逐文件未跟踪条目 10,334 → 211。<br>⚠️ **但发现更值得注意的一件事**:`experiments/` 下**一个文件都没入库**(`git ls-files experiments` = 0),包括**防线命令依赖的** `duration_audit/reaggregate_normalized.py` —— 误删即永久丢失。建议把 `experiments/duration_audit/`(336 KB,脚本 + README)单独入库,属论文线资产,等你点头 |
| 2 | **M5 的 L1 阈值许可问题** | 见 §5 第 1 条,已在设计文档 §10 记一条约束 |
| 3 | **"缺断言"这一类怎么防** | 目前无自动手段(`sys.settrace` 扫描抓不到)。可考虑:对 docstring 声明与断言数量的弱检查,或接受人工审查 |
| 4 | `data/output/` 里的旧报告 | 已清 42 份(修复前生成的);根 `app.py` 的 `/output/` 路由仍可访问 `data/output/`,现在都是新报告 |

---

## 7. 关键文件索引

| 用途 | 路径 |
|---|---|
| **本文件** | `docs/下一步.md` |
| ① 的设计增补(权威) | `docs/superpowers/specs/2026-09-22-jingxin-report-layer-evidence-gate-design.md` |
| ① 的实施计划 | `docs/superpowers/plans/2026-09-22-report-layer-evidence-gate.md` |
| 特征重构设计文档 | `docs/superpowers/specs/2026-09-21-jingxin-feature-redesign-design.md` |
| 心理测量学审查(原始) | `docs/superpowers/specs/2026-09-22-测评回复_心理测量学审查.md` |
| 录制需求(三个硬前提) | `docs/superpowers/specs/2026-09-22-jingxin-recording-requirements.md` |
| **① 的账本与未决清单** | `docs/superpowers/sdd/2026-09-22-report-layer-evidence-gate/` |
| 证据门实现 | `report_frontend/evidence_gate.py` + `evidence_thresholds.json` |
| 报告层入口 | `report_frontend/research_mapper.py` / `report_generator.py` / `visualizer.py` / `feature_engine.py` |
| 测试 | `tests/`(4 个文件) |

**解释器固定为** `~/miniconda3/envs/jingxin/bin/python`。不能用 `~/huihui/bin/python` —— `report_frontend/__init__.py` 会连带导入 `visualizer`,依赖 plotly,只有 jingxin 环境有。

**合并门/防线(任何时候都要能过)**:

```bash
cd ~/jingxin/experiments/duration_audit
PY=~/miniconda3/envs/jingxin/bin/python
$PY reaggregate_normalized.py --stats legacy --out-dir /tmp/legacy_probe
$PY reaggregate_normalized.py --verify-legacy /tmp/legacy_probe   # 期望 0/2835510
```

---

## 8. 一句话提醒

**系统该宣称的是「结构化行为记录 + 低置信度特质提示」,不是「心理识别结论」。** 精度天花板(r ≈ 0.33)不在这一轮的目标里;① 让系统停止说谎,M1–M3 让它重新有真话可说。
